# jira-kan

Local shadcn Kanban for [jira-cli](https://github.com/ankitpokhrel/jira-cli) JSON.

Pipe `jira issue list --raw` in, or Refresh from the UI. Drop a Card on a Column to `jira issue move`. Click a Card to `jira open`.
